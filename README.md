Grid::grid_serialized has a coordinate system defined like images in opencv: (x,y)=(0,0) is the top left corner. x counts to the right, y counts downwards.

Due to firewall issues, I couldnt download visual studio tools or source files for glfw. Therefore precompiled binaries are provided. They will not work on all architectures and must be configured per system.